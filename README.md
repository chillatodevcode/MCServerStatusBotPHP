# MCServerStatusBotPHP

# Requirements:
- Have php installed
- Have composer installed

# Linux install:
```bash
sudo apt install php
```
```bash
sudo apt install php-curl
```
```bash
sudo apt install composer
```

# Composer Command:
```bash
composer require skrtdev/novagram ^1.9
```
